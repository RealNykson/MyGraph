<UserControl
    x:Class="MyGraph.Views.TransferUnit"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:MyGraph.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MyGraph.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:MyGraph.ViewModels"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Border Background="Red" Width="{Binding Width}" Height="{Binding Height}" BorderBrush="Black" BorderThickness="1" CornerRadius="3">
        <Grid>
            <TextBlock Margin="0,10,0,0" Text="{Binding Name}" VerticalAlignment="Top" HorizontalAlignment="Center" />
        </Grid>
    </Border>
</UserControl>

